apiVersion: v1
kind: Service
metadata:
  name: app1
  namespace: devops-challenge
  labels:
    app: app1
spec:
  selector:
    app: app1
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP  # Acesso apenas interno (via nginx)
